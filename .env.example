# Copiar a .env (ignorado por git). Solo lo usa docker-compose.yml.
# Basta una de las dos claves para dsh.
DEEPSEEK_API_KEY=
# Suscripción OpenCode Go. Requiere instalar tools/dsh/opencode-go.patch.yml en
# el perfil de dsh (ver la skill global dsh-harness).
OPENCODE_API_KEY=
